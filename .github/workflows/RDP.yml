name: Linux Test

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Check Linux environment
        shell: bash
        run: |
          echo "Hostname: $(hostname)"
          echo "User: $(whoami)"
          uname -a

      - name: Run tests
        shell: bash
        run: |
          echo "Running Linux tests..."
          # Put your build/test commands here

      - name: Keep runner alive briefly
        shell: bash
        run: |
          sleep 300
